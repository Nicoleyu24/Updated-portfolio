Create a new React component for the portfolio site.

Component name: $ARGUMENTS

Follow these conventions:

1. Create the file at `src/components/<ComponentName>.tsx`
2. Use TypeScript with explicit prop types defined as an interface above the component
3. Use Tailwind utility classes for all styling — no inline styles
4. Default to a server component (no "use client" directive) unless the component needs interactivity
5. Use a named export (not default export)
6. Brand colors: orange `#e13400` for accents, `#1a1a1a` for text, `#666` for muted text
7. After creating it, show me how to import and use it in page.tsx

Usage: /component HeroSection
