Create a new page for the portfolio site.

Page name / route: $ARGUMENTS

Follow these conventions:

1. Create the directory and file at `src/app/<route>/page.tsx`
2. Import and use the Navbar and Sidebar components from `@/components/`
3. Wrap content in a `<main>` with `className="ml-[200px] pt-[100px] p-10"`
4. Use TypeScript
5. Use Tailwind for all styling
6. Keep the same clean, minimal aesthetic as the homepage

Usage: /page projects/healthcare
